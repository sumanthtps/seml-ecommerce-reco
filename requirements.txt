# Pinned runtime dependencies for reproducible container builds.
# Dev/test/lint tools live in pyproject.toml under [project.optional-dependencies].
# Pins match the versions validated locally; bump intentionally.
fastapi==0.137.2
uvicorn[standard]==0.49.0
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.2
numpy==2.4.6
scikit-learn==1.9.0
